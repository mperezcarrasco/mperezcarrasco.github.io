# Images to upload for the MethaneSAT blog page

Drop the files below into this `methanesat/images/` folder using **exactly these names**.
Until then, the page shows a labeled dashed placeholder in each slot (nothing breaks).

| Filename | What it should be | Source |
|---|---|---|
| `xch4_maps.png` | Side-by-side XCH₄ maps: MethaneAIR (10 m) vs. MethaneSAT (45 m), same basin | Plume preprint Fig. 1 (`xch4_maps.png` in Dataverse DVN/FR959H) |
| `scan_architecture.png` | SCAN schematic: cube → averaged spectrum → per-band attention → classifier → mask | NeurIPS 2025 SCAN paper, Fig. 3 |
| `cloud_predictions.png` | Qualitative grid: input / ground truth / SCAN / Combined CNN cloud+shadow masks | TGRS / NeurIPS paper (model-comparison figure) |
| `plume_pipeline.png` | 5-step plume pipeline diagram (input → patches → transfer → postprocessing → output) | Plume preprint Fig. 3 (`Plumes_paper.png` in Dataverse DVN/FR959H) |
| `plume_scene.png` | Full MethaneSAT scene: input XCH₄ → ground truth → predicted plume outlines | Plume preprint Fig. 7 (`full_scene_*.png` in Dataverse DVN/FR959H) |

Optional: replace any of these later — the page just needs the filename to match.
